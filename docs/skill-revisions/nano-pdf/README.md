# nano-pdf verification guidance

OPS-239 source package for two general-only instruction corrections. Establish page indexing before editing a copy; verify page text and rendering, not file size. All other domain content is preserved. This is not a new installed skill.

Run `python3 test_revision.py -v` here. Four deterministic text and isolated rollback checks do not invoke nano-pdf or any model and are not PDF behavior acceptance. `baseline.md` is the exact rollback source. No other-profile deployment is authorized by this package.
